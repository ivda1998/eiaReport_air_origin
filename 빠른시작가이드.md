# 환경영향평가 대기질 보고서 자동화 시스템 - 빠른 시작 가이드

## 개요

이 시스템은 AERMOD 대기확산 모델링 결과를 바탕으로 환경영향평가 보고서를 자동으로 작성하는 도구입니다.

## 기존 프로젝트 vs 새 프로젝트

### 🔹 기존 프로젝트 사용하기

이미 설정된 프로젝트가 있다면:

```bash
cd G:\eia
python t2_v2.py
```

### 🔹 새 프로젝트 만들기

새로운 환경영향평가 사업을 시작한다면:

```bash
python init_project.py
```

대화형 프롬프트를 따라 프로젝트 이름, 경로 등을 입력하면 자동으로 프로젝트 구조가 생성됩니다.

## 파일 구조

```
프로젝트폴더/
├── config.yaml              ⭐ 설정 파일 (API 키, 경로 등)
├── t2_v2.py                ⭐ 메인 스크립트
├── init_project.py         🔧 새 프로젝트 생성 도구
├── requirements.txt        📦 필요한 라이브러리 목록
├── README.md              📖 상세 사용 설명서
│
├── input/                 📝 AERMOD 입력/출력
│   ├── project.inp
│   └── project.out
│
├── output/                💾 생성된 보고서 (자동 생성)
│   ├── 대기질_환경영향평가서.txt
│   ├── 대기질_환경영향평가서.docx
│   └── 등농도곡선_PM2.5.png
│
├── sample/                📚 참조할 기존 보고서 (PDF)
│
├── data/                  🗂️ 프로젝트 데이터
│   ├── base.dxf          (베이스맵)
│   ├── 정온시설.xlsx
│   ├── 현황측정자료.xlsx
│   └── references/       (논문, 보고서 등)
│
└── engine/                ⚙️ AERMOD 실행 파일
    └── aermod.exe
```

## 1단계: 설치

### 필수 라이브러리 설치

```bash
pip install pandas numpy matplotlib scipy ezdxf PyPDF2 python-docx pyyaml google-genai
```

또는 requirements.txt 사용:

```bash
pip install -r requirements.txt
```

### Google Gemini API 키 발급

1. https://aistudio.google.com/apikey 방문
2. "Create API Key" 클릭
3. 발급된 키를 복사

## 2단계: 설정

### config.yaml 수정

프로젝트 폴더의 `config.yaml` 파일을 텍스트 에디터로 열어 다음 항목을 수정:

```yaml
# API 키 입력 (필수!)
ai:
  api_key: "여기에_발급받은_API키_입력"

# 경로 확인
paths:
  base_dir: "G:\\eia"  # 실제 프로젝트 경로

# 파일명 확인 (다르면 수정)
data_files:
  base_map: "base.dxf"
  facilities: "정온시설.xlsx"
  measurements: "현황측정자료.xlsx"
```

### 정온시설 엑셀 구조 확인

정온시설 엑셀 파일을 열어서:
- 데이터가 몇 번째 행부터 시작하는지
- 시설명, X좌표, Y좌표가 몇 번째 열(컬럼)에 있는지 확인

config.yaml에서 해당 부분 수정:

```yaml
data_files:
  facilities_structure:
    header_rows: 2      # 헤더 행 수
    name_column: 3      # 시설명 컬럼 (0부터 시작)
    x_column: 7         # X 좌표 컬럼
    y_column: 8         # Y 좌표 컬럼
```

**예시**: 엑셀에서 A열=0, B열=1, C열=2, D열=3, ...

## 3단계: 파일 준비

다음 파일들을 해당 폴더에 복사:

### 필수 파일 ⭐
- ✅ `input/project.inp` - AERMOD 입력 파일
- ✅ `engine/aermod.exe` - AERMOD 실행 파일
- ✅ `data/base.dxf` - 베이스맵 DXF 파일
- ✅ `data/정온시설.xlsx` - 정온시설 위치 정보
- ✅ `data/현황측정자료.xlsx` - 현황 측정 자료

### 선택 파일 (보고서 품질 향상) 📈
- 📄 `sample/*.pdf` - 기존 환경영향평가서 (양식 학습용)
- 📄 `data/references/*.pdf` - 관련 논문, 정부 보고서 (근거 자료)

## 4단계: 실행

### 명령어 실행

```bash
python t2_v2.py
```

### 메뉴 선택

```
[명령어]
  1. 모델링   - AERMOD 시뮬레이션 실행
  2. 분석     - 결과 분석 및 보고서 작성 (TXT)
  3. 전체     - 모델링 + 분석 한번에 실행 (TXT)
  4. DOCX     - 보고서를 DOCX 형식으로 작성
  5. 등농도선 - 등농도곡선만 생성
  6. 종료     - 프로그램 종료

명령을 입력하세요:
```

### 추천 워크플로우

**처음 사용하는 경우:**
1. 명령어 `3` (전체) 선택 → 모델링 실행 + TXT 보고서 생성
2. 명령어 `4` (DOCX) 선택 → DOCX 파일 생성
3. 명령어 `5` (등농도선) 선택 → 등농도곡선 설정 조정 및 재생성

**이미 모델링을 실행한 경우:**
1. 명령어 `2` (분석) 또는 `4` (DOCX) 선택

## 5단계: 결과 확인

### 생성된 파일

- 📄 `output/대기질_환경영향평가서.txt` - 텍스트 보고서
- 📄 `output/대기질_환경영향평가서_YYYYMMDD_HHMMSS.docx` - 워드 문서
- 🖼️ `output/등농도곡선_PM2.5.png` - 등농도곡선 이미지

### DOCX를 HWP로 변환

1. 한글(HWP) 프로그램에서 DOCX 파일 열기
2. "파일 > 다른 이름으로 저장 > 파일 형식: HWP" 선택
3. 저장

## 자주 묻는 질문 (FAQ)

### Q1. API 키 오류가 발생합니다

**증상:**
```
❌ 설정 초기화 실패
```

**해결:**
- `config.yaml` 파일을 열어 `ai.api_key` 항목에 유효한 API 키가 입력되어 있는지 확인
- API 키는 https://aistudio.google.com/apikey 에서 발급

### Q2. AERMOD 출력 파일을 찾을 수 없다고 나옵니다

**증상:**
```
❌ AERMOD 출력 파일을 찾을 수 없습니다
```

**해결:**
- 먼저 명령어 `1` (모델링)을 실행하여 AERMOD 시뮬레이션 수행
- `input/project.inp` 파일이 존재하는지 확인
- `engine/aermod.exe` 파일이 존재하는지 확인

### Q3. 등농도곡선에 베이스맵이 표시되지 않습니다

**증상:**
```
⚠️ DXF 파일을 찾을 수 없습니다
```

**해결:**
- `data/base.dxf` 파일이 존재하는지 확인
- `config.yaml`의 `data_files.base_map` 항목이 올바른 파일명인지 확인

### Q4. 정온시설이 지도에 표시되지 않습니다

**해결:**
1. `data/정온시설.xlsx` 파일을 엑셀에서 열기
2. 데이터가 몇 번째 행부터 시작하는지, X/Y 좌표가 몇 번째 열에 있는지 확인
3. `config.yaml`의 `data_files.facilities_structure` 수정:
   ```yaml
   facilities_structure:
     header_rows: 2      # 데이터가 3번째 행부터면 2
     name_column: 3      # D열이면 3 (A=0, B=1, C=2, D=3)
     x_column: 7         # H열이면 7
     y_column: 8         # I열이면 8
   ```

### Q5. DOCX 파일 저장 실패

**증상:**
```
❌ DOCX 파일 저장 실패 - 권한 오류
```

**해결:**
- 기존에 생성된 DOCX 파일이 Word나 한글에서 열려있다면 닫기
- `output` 폴더의 기존 DOCX 파일 삭제 또는 이름 변경

### Q6. 등농도곡선 간격/범위를 조정하고 싶습니다

**방법 1**: 명령어 `5` (등농도선) 선택 후 대화형으로 설정

**방법 2**: `config.yaml`에서 기본값 수정
```yaml
isopleth:
  default_level_min: 0.0      # 최소 레벨
  default_level_max: 50.0     # 최대 레벨
  default_level_count: 10     # 레벨 개수
  default_level_interval: 5.0 # 간격 (5 μg/m³ 간격)
```

### Q7. 보고서 품질을 높이고 싶습니다

**팁:**
1. **샘플 보고서 추가**: `sample` 폴더에 유사한 기존 환경영향평가서(PDF) 추가
   - AI가 양식과 작성 스타일을 학습합니다

2. **참조 문헌 추가**: `data/references` 폴더에 관련 논문, 정부 보고서, 유사 사업 평가서 추가
   - 보고서에서 선행 연구를 인용하여 신뢰성이 높아집니다

3. **현황 데이터 충실**: 정온시설 정보와 현황측정자료를 정확하게 제공

### Q8. 다른 오염물질을 추가하고 싶습니다

`config.yaml`의 `pollutants`에 항목 추가:

```yaml
pollutants:
  - name: "CO"              # AERMOD에서 사용하는 이름
    display_name: "CO"      # 보고서에 표시될 이름
    unit: "ppm"             # 단위
    standard: 9.0           # 환경기준 (8시간 평균)
```

## 고급 사용법

### 등농도곡선 시각화 커스터마이징

`config.yaml`의 `isopleth.visualization` 섹션에서 색상, 크기, 투명도 등을 조정:

```yaml
visualization:
  figure_width: 14          # 그림 너비
  figure_height: 12         # 그림 높이
  dpi: 300                  # 해상도 (높을수록 선명)
  contour_color: "blue"     # 등농도선 색상
  facility_color: "red"     # 정온시설 색상
  base_map_alpha: 0.5       # 베이스맵 투명도 (0~1)
```

**색상 옵션**: "blue", "red", "green", "black", "purple", "orange" 등

### 여러 오염물질 동시 분석

AERMOD 입력 파일에 여러 오염물질이 포함되어 있다면, 각 오염물질에 대해 자동으로 등농도곡선이 생성됩니다.

## 문제 해결 체크리스트

실행 전 확인:
- [ ] Python 3.7 이상 설치
- [ ] 필수 라이브러리 설치 완료
- [ ] config.yaml에 API 키 입력
- [ ] input/project.inp 파일 존재
- [ ] engine/aermod.exe 파일 존재
- [ ] data/base.dxf 파일 존재
- [ ] data/정온시설.xlsx 파일 존재
- [ ] config.yaml의 경로 설정 확인

## 지원

- **상세 가이드**: README.md 파일 참조
- **설정 파일**: config.yaml 주석 참조
- **프로젝트 초기화**: `python init_project.py` 실행

---

**버전**: 2.0
**최종 업데이트**: 2025-01-21
