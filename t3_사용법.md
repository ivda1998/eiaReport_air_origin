# t3.py 사용 가이드 (Claude AI 버전)

## ✅ 설치 완료

`anthropic` 패키지가 정상적으로 설치되었습니다!

## 🔑 API 키 설정 (필수)

t3.py를 실행하기 전에 Claude API 키를 설정해야 합니다.

### 방법 1: 환경변수로 설정 (권장)

**Windows 명령 프롬프트:**
```cmd
set ANTHROPIC_API_KEY=your-api-key-here
python t3.py
```

**Windows PowerShell:**
```powershell
$env:ANTHROPIC_API_KEY="your-api-key-here"
python t3.py
```

### 방법 2: 코드에 직접 입력

`t3.py` 파일의 22번째 줄을 수정:

```python
ANTHROPIC_API_KEY = "여기에-당신의-API-키-입력"
```

## 🚀 실행 방법

```bash
python t3.py
```

## 📋 사용 가능한 명령어

프로그램 실행 후:

1. **모델링** - AERMOD 시뮬레이션만 실행
2. **분석** - 기존 결과를 분석하여 TXT 보고서 작성
3. **전체** - 모델링 + 분석 한번에 실행 (TXT)
4. **DOCX** - DOCX 형식으로 보고서 작성
5. **종료** - 프로그램 종료

## 📂 출력 파일

- `output/대기질_환경영향평가서_Claude.txt` - 텍스트 보고서
- `output/대기질_환경영향평가서_Claude_YYYYMMDD_HHMMSS.docx` - 워드 문서

## ⚠️ 주의사항

1. **API 키 필수**: Claude API 키 없이는 실행되지 않습니다
   - API 키는 https://console.anthropic.com/ 에서 발급받을 수 있습니다

2. **레이트 리미트**: API 호출 제한에 걸리면 60초 대기 후 자동 재시도합니다

3. **파일 충돌**: DOCX 파일 생성 시 기존 파일이 열려있으면 타임스탬프가 추가된 새 파일로 저장됩니다

## 🆚 t2.py vs t3.py 비교

| 항목 | t2.py (Gemini) | t3.py (Claude) |
|------|----------------|----------------|
| AI 모델 | Google Gemini 2.5 Flash | Claude Sonnet 4 |
| 출력 파일명 | 대기질_환경영향평가서.txt | 대기질_환경영향평가서_Claude.txt |
| API 키 | GOOGLE_API_KEY | ANTHROPIC_API_KEY |
| 속도 | 빠름 | 보통 |
| 품질 | 좋음 | 우수 |
| 분석 깊이 | 표준 | 심층 |

## 💡 추천 워크플로우

1. **t2.py로 빠른 테스트**
   ```bash
   python t2.py
   # 명령: 3 (전체)
   ```

2. **t3.py로 고품질 보고서 생성**
   ```bash
   python t3.py
   # 명령: 3 (전체)
   ```

3. **결과 비교 후 선택**
   - output 폴더에서 두 파일을 비교
   - 더 나은 버전을 최종 보고서로 사용

## 🐛 트러블슈팅

### "ANTHROPIC_API_KEY를 설정하세요" 오류
→ API 키를 환경변수로 설정하거나 t3.py 파일 22번째 줄에 직접 입력

### "Rate limit exceeded" 오류
→ 자동으로 60초 대기 후 재시도됩니다. 잠시 기다려주세요.

### DOCX 파일 저장 실패
→ 기존 DOCX 파일을 닫아주세요. 또는 자동으로 새 파일명으로 저장됩니다.

## 📞 문의

문제가 있으면 에러 메시지와 함께 문의해주세요.
